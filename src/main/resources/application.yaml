server:
  port: 9091
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: 485010
    url: jdbc:mysql://localhost:3306/emergency_material_manage?serverTimezone=GMT%2b8
    username: root
    
  task:
    scheduling:
      pool:
        size: 10
    

mybatis:
  mapper-locations: classpath:mapper/*xml

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    
files:
  upload:
    path: D:\Emergency_Material_Manage\files\
